#include <blPipelineState.h>

namespace BoulderLeaf::Graphics::DX12
{
	blPipelineState::blPipelineState(std::shared_ptr<blDevice> device)
		: mDevice(device)
	{
	}

	void blPipelineState::SetState(blMaterial& material)
	{

	}
}